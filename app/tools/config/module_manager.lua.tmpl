-- auto generated by format_lua_module.py
#for k, v in $mInfo.items()

${v['desc']}
#end for


local modulePathMap = {
#for k, v in $mInfo.items()
    ['${k}'] = '${v['path']}',
#end for
}

---@class module_manager
#for k, v in $mInfo.items()
---@field ${k} ${k}
#end for
module_manager = setmetatable({}, {
    __index = function(_, module_name)
        local modulePath = modulePathMap[module_name]
        assert(is_valid_str(modulePath))
        return import(modulePath)
    end,
})
