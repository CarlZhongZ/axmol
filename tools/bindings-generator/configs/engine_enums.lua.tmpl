-- auto generated by tools

#for enumType in $enumTypes
    #set enumInfo = $parsedEnums[enumType]

${enumInfo.luaClassName} = {
    #for m in enumInfo.fields
    ${m[0]} = ${m[1]},
    #end for
}
#end for
