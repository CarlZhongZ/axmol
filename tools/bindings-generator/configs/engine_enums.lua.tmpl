-- auto generated by tools

engine_enums = {}
#for enumType in $enumTypes
    #set enumInfo = $parsedEnums[enumType]

---@type ${enumInfo.luaClassName}Table
engine_enums.${enumInfo.luaClassName.replace('.', '_')} = {
    #for m in enumInfo.fields
    ${m[0]} = ${m[1]},
    #end for
}
#end for
