-- auto generated by tools

-- enum types
#for enumType in $enumTypes
    #set enumInfo = $parsedEnums[enumType]
---@alias ${enumInfo.luaClassName} number

---@class ${enumInfo.luaClassName}Table
    #for m in enumInfo.fields
---@field ${m[0]} number
    #end for
#end for

#for structType in $structTypes
    #set structInfo = $parsedStructs[structType]
    #if not structInfo.isNotSupported

-- struct:${structInfo.ns_full_name}
---@class ${structInfo.luaClassName}
        #for m in structInfo.fields
            #set desc = m.luaFieldDesc
            #if desc
${desc} @ field
            #end if
        #end for
    #end if
#end for

#for classType in $classTypes
    #set classInfo = $generated_classes[classType]

-- class:${classInfo.ns_full_name}
    #if classInfo.parents
---@class ${classInfo.luaClassName}: ${classInfo.parents[0].luaClassName}
    #else
---@class ${classInfo.luaClassName}
    #end if
    #for m in classInfo.public_fields
        #set desc = m.luaFieldDesc
        #if desc
${desc} @ field
        #end if
    #end for
    #for m in classInfo.static_methods.items()
        #set desc = m[1].luaFieldDesc
        #if desc
${desc} @ static method
        #end if
    #end for
    #for m in classInfo.methods.items()
        #set desc = m[1].luaFieldDesc
        #if desc
${desc} @ method
        #end if
    #end for
#end for
